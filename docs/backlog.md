# Backlog

Out-of-scope items noted during batch work. One line each. Do not fix inline — pick up in the batch noted (or a new one) with its own plan + approval.

- **Duplicate `<title>`/`<h1>` in SSG output** — Batch 0A's SSG proof-of-concept (`client/scripts/ssg-build.js`) injects real rendered content + Helmet head tags into the raw `dist/index.html` SPA shell, which still carries its own default `<title>`/meta block and a `<noscript>` fallback with its own `<h1>`. Result: 2 `<title>` and 2 `<h1>` in raw HTML source for all 3 SSG routes (crawlers executing JS see only the correct ones, since `<noscript>` is suppressed when scripting is enabled — but a raw HTML-only parser would see both). Needs: replace-not-append head injection, and dropping the now-redundant `noscript` fallback once SSG is the real serving path. Belongs in the batch that wires SSG output for actual production serving (not this POC).
- **Google Search Console — request re-indexing for key routes** now that SSG output is live (owner action, not code).
- **App.css migration to design-system tokens** — `client/src/App.css` is legacy "Resume Builder — Dark Professional Theme" CSS with bare `body`/global overrides (dark background, Inter font) that collides with Blueprint §3/§4's light paper/ink theme. Batch 1's `globals.css` is kept additive-only (no bare element selectors) specifically to avoid this collision; retiring App.css in favor of token-driven global typography is real work for whichever batch migrates components off legacy CSS.
- ~~Hardcoded adsbygoogle.js script tag ships on every page~~ — **corrected 2026-07-12**: verified on production that both AdSense `<script>` blocks in `client/index.html` are fully wrapped in HTML comments and never load. The real gap (manual comment-toggling instead of the env-var flip) is fixed in Batch 1C.
- **Global `:focus-visible` ring not adopted site-wide** — both reference HTML files use a single global `:focus-visible{outline:3px solid var(--cobalt);outline-offset:2px;border-radius:4px}` rule. Batch 2's new `Button` primitive scopes this to itself only (adding it to `globals.css` globally would visibly change keyboard-focus rendering on all 129 live routes today). Adopt the global rule once existing interactive elements are ready for the redesign.
- **`ui/` vs `primitives/` naming overlap** (`Button`, `Badge`, `Container` exist in both, Batch 2) — needs a dedicated adoption batch that migrates existing pages off `ui/` and retires the duplicates, rather than each new batch picking one ad hoc.
- **Batch 2 primitives not visually verified in a browser** — no browser-automation tool was available; verification was static (compiled-CSS inspection against reference file values). Someone should open each primitive against `docs/reference/*.html` before later batches build on top of them, especially Ledger and Callout (§2's "signature" elements).
- **Store nav item kept despite absence from Blueprint §15 / architecture.md §2's nav lists** (Batch 3) — restyled into the header/mobile nav as a regular link per user decision 2026-07-13, revisit after Batch 5 (homepage) shows the fuller IA with Store present.
- **5 missing footer policy routes** (`/editorial-policy`, `/corrections-policy`, `/ai-content-policy`, `/advertising-policy`, `/tool-testing-policy`), plus `/request-tool` and the two `/blog/category/*` routes referenced by the frozen footer spec (Blueprint §18, reference `awe-os-homepage.html`) — Batch 3's Footer ships only the 3 policy routes that exist today (`/privacy`, `/terms`, `/disclaimer`). Deferred to Batch 8.
- **Breadcrumb primitive (Batch 2) not yet adopted anywhere** — Blueprint §15 calls for it on every page except the homepage; that's a page-level concern for the batches that build those page templates (Batch 4+), not Header/Footer.
- **`docs/reference/claude-code-execution-playbook.md`'s Batch 3 prompt references `content/site.ts`**, which doesn't exist in this Vite/React stack (no `content/` directory at all). Same category of Next.js-flavored leftover language as the architecture.md mismatch already noted in CLAUDE.md's changelog. Nav content stays hardcoded in `Header.jsx`/`Footer.jsx`, consistent with existing code — not fixing the frozen playbook doc.
- ~~Footer bottom-bar text contrast is marginally under WCAG AA~~ — **resolved in batch 3**: per user ruling 2026-07-13 (Blueprint §21's AA floor wins over reference-value fidelity), `--footer-text-dim` raised from the reference's literal `rgba(255,255,255,.45)` (≈4.43:1 on `--ink`) to `rgba(255,255,255,.55)` (≈5.98:1). Re-verified via Lighthouse on `/`: the 2 footer color-contrast findings are gone post-fix.
- **`AdBanner.jsx`'s ad-placeholder wrapper has `aria-label="Advertisement"` on a bare `<div>` with no role** (pre-existing, found incidentally during Batch 3's Lighthouse spot-check — axe's `aria-prohibited-attr` rule flags this). Only renders when `ADS_ACTIVE` is true, which is off in production (Batch 1C), so this is currently dormant and unrelated to Batch 3's diff — logged here since it was noticed, not fixed (out of scope; will matter again whenever ads are activated per Blueprint §24).
- **`client/.env` is untracked (not in git)** — confirmed while verifying Batch 4's byte-identical build: a fresh `git worktree` checkout has no `.env`, so `VITE_ADSENSE_PUBLISHER_ID`'s local placeholder value is absent there, making `ADS_ACTIVE` false in a clean checkout vs. true in this machine's working copy. Not a code issue — local dev environments should document what `.env` values are expected (`.env.example` already exists) so this doesn't cost verification time again in a future batch.
- **`cards/ToolCard.jsx` naming overlap with `client/src/components/ToolCard.jsx`** (Batch 4) — same deliberate-duplication situation as Batch 2's `ui/`-vs-`primitives/` overlap; a future adoption batch decides how `ToolsPage.jsx`/`ToolDetailPage.jsx` migrate off the old one.
- **Header's raw `.95rem` nav-link text (Batch 3) could adopt the new `--text-md` token (Batch 4)** for consistency — optional cleanup, not required, since both currently resolve to the same value.
- **Site-wide `motion-reduce` / `:focus-visible` rules still not adopted globally** — Batch 4's card hover states are the third batch (after Batch 2's `Button` and Batch 3's mobile menu) to scope motion/focus handling to the component rather than `globals.css`, for the same reason each time (avoiding a visible change across all 129 live routes today). Adopt the global rules once existing interactive elements are ready for the redesign.
- **Internal surfaces now render in system fonts** (Batch 5.5 stage 1b) — removing the legacy Inter `<link>` from `client/index.html` (render-blocking, no public component used it) means `AppShell`/`AdminShell` and any other page still relying on `App.css`'s unscoped `body { font-family: 'Inter' }` (e.g. AI Resume Builder, standalone Calculators) now fall back to `system-ui`. Accepted per user ruling 2026-07-14 — internal tools' font aesthetics aren't a protected property. Optionally lazy-load Inter inside the internal shell later if the team wants it back.
- **`client/src/main.jsx` uses `createRoot(...).render()`, not `hydrateRoot`** — found while diagnosing Batch 5.5's CLS root cause; confirmed as the actual cause (not the font-swap or auth-gate theories tried first) via a live CDP probe showing the `<footer>` collapsing to an exact `{0,0,0,0}` rect on every load. The client never actually hydrates the SSG-rendered HTML; it discards it and does a fresh client-side render every time. Assigned its own batch — **Batch 5.6, SSG hydration fix** — per user ruling 2026-07-14, since the blast radius is all 129 routes and every component's hydration-safety assumptions.
- ~~Desktop a11y: touch-target sizing flagged~~ — **checked in Batch 5.5 Stage 2**: Lighthouse's `target-size` audit scores 1/1 (zero failing elements) on the homepage. Not reproduced there — if it's real, it's on a different page. Needs the specific URL from the user's DevTools session before it can be diagnosed further.
- **`Badge.jsx`'s icon has the same low-contrast `text-mint` pattern Batch 5.5 fixed in `Chip.jsx`/`Ledger.jsx`** — not touched since `Badge` isn't rendered anywhere on the homepage (out of this batch's scope). Apply the same `text-mint-text-strong` swap whenever `Badge` is actually adopted somewhere.
- **`Footer.jsx`'s social icon links fail axe's `label-content-name-mismatch`** — found incidentally during Batch 5.5 Stage 2's a11y scan, not one of the 4 requested categories. `<a aria-label="Twitter">𝕏</a>` (and likely the LinkedIn/GitHub siblings): visible glyph isn't contained in the accessible name, so voice-control users saying "click Twitter" get a match but users reading the visible "𝕏" and expecting that exact string in the name may not. Arguable (the glyph is a brand mark, not literal text) — logged, not fixed, since it wasn't in scope.
- **CSP (Batch 5.5 stage 3) needs `'unsafe-inline'` for `script-src`/`style-src`** — GA4's inline bootstrap `<script>` in `index.html`, and the app's extensive use of React inline `style={{}}` throughout the codebase. Shipped Report-Only this batch (ruling), so this is a real gap to close (nonce/hash-based script-src, moving inline styles to classes) before ever flipping the CSP to enforcing. **Update (Batch 16)**: GA4's inline bootstrap is gone (swapped for Umami Cloud's external, non-inline script tag) — but `script-src 'unsafe-inline'` is still required by the 22 files rendering `<script type="application/ld+json">` JSON-LD schema blocks (`client/src/utils/schema/*`), unrelated to GA4. `style-src 'unsafe-inline'` is unchanged (React inline `style={{}}`). Enforce-flip remains blocked on those two, not on GA4.
- **`ssg-build.js` doesn't resolve nested `<Suspense>`/`lazy()` boundaries** — found while attempting Batch 5.5 Stage 4's "lazy-wrap `Guides`" quick win. Route-level lazy boundaries resolve correctly in SSG output (proven: every page in `routes.jsx` is already `lazy()`-wrapped and all 129 routes render full content) — but a *second*, nested `<Suspense>` boundary inside an already-lazy page bakes the fallback into the static HTML instead of the real content. Confirmed via `Guides` specifically; reverted rather than ship crawler-invisible content. Same family of issue as the `createRoot`-vs-`hydrateRoot` finding — likely belongs in **Batch 5.6**. Until fixed, `client/src/data/blogPosts.js` (376KB/112KB gzip) stays in the homepage's eager bundle; the only other route to the same win is the already-backlogged data-layer split (metadata index + lazy per-post content).
- **2026-07-15** — `client/node_modules` partially committed to git (~9400 files, seen in Vercel clone logs) — needs `.gitignore` fix + `git rm --cached` in a cleanup batch (fold into 0C).
- ~~**Prod bug, `/tools` "All" tab shows 0 tools**~~ — **fixed 2026-07-15 (Batch 5.6c)**: `server/scripts/sync-tool-registry.js` (2026-07-03, commit `18a42e7`) inserted shipped tools into `tools` with `status='live'` but never `approved=true` — the column `GET /api/tools/public` actually filters on. Category tabs were unaffected (separate static file, not this table). Confirmed pre-existing, unrelated to batch 5.6. Data fix: owner ran `UPDATE tools SET approved=true WHERE status='live' AND approved=false` (16 rows). Code fix: `sync-tool-registry.js`'s `toRow()` now sets `approved: true` for future runs. See `docs/batches/batch-5.6c-tools-approved-flag.md`.
- ~~**Prod bug, `GET /api/calculators` returns HTTP 500**~~ — **fixed 2026-07-27**: (`column calculators.approved does not exist`), confirmed live via `curl https://awe-os.onrender.com/api/calculators`. `server/routes/calculators.routes.js:21` and `:33` did `.eq('approved', true)` against a `calculators` table with no such column — same bug class as the `tools.approved` fix above, but unlike that one, the owner chose to drop the filter rather than add the missing column/migration. Both `.eq('approved', true)` calls removed; public endpoints now return all rows in `calculators`. `CalculatorsListPage.jsx` should render real content again — not re-verified live post-deploy.
- **2026-07-15** — **Confirmed root cause (Batch 5.6c): registry-vs-pipeline slug collision** — only 15 of the registry's 49 tools have a resolved `live` path (16th, `resume-builder`, has a duplicate stale row) because the AI pipeline and the real tool registry share one table and one slug namespace, and `sync-tool-registry.js`'s dedup (`fetchExistingSlugs()`) matches on slug alone — so a stale AI-pipeline row (`status=killed`/`building`, `source=ai`) permanently blocks the real tool from ever getting its own `live` row. Confirmed via read-only diagnostic query: 32 slugs blocked this way, `resume-builder` has two rows for one slug (its `live` row predates the sync entirely), `pdf-editor` is `needs_fix`, `roi-calculator` is `idea` (15+32+1+1=49 ✓). Full CSV evidence and candidate fix directions (scoped dedup by slug+source, slug-uniqueness decision, or reading `toolCatalogue.js` for the "All" tab too) in `docs/batches/batch-5.6c-tools-approved-flag.md`'s open-question section — needs its own scoped batch with owner ruling on direction, not designed yet.
- **2026-07-15** — **Batch 5.6b needed**: an early-effect-vs-Suspense-hydration race (React #421/#422, "update received before it finished hydrating") blocks `hydrateRoot` on all 48 individual `/tools/:slug` pages and 3/41 blog posts — confirmed via a rigorous 3-run determination sweep, root cause not yet isolated (one contributing cause found and fixed in `ToolPageShell.jsx`, didn't fully clear it). Batch 5.6 shipped `hydrateRoot` only for the 39 routes proven reliable (home, tool category pages, static pages, all 24 city pages, compare, faq); tool pages and blog stay on `createRoot` (today's pre-batch behavior, no regression) until this is isolated. Bisection starting point recorded in `docs/batches/batch-5.6-ssg-hydration.md`'s "Batch 5.6b" section. **Update (observed 2026-07-15, batch 8b's verification sweep)**: city pages, previously 3/3 clean in batch 5.6's determination sweep and part of the hydrate-safe set, are now also intermittently hitting this same #421 race — `/bmi-calculator/mumbai`, `/sip-calculator/mumbai`, `/sip-calculator/pune` failed one hydration sweep run. Not a new defect and not caused by any file touched in batch 8b — same timing-sensitive race, now manifesting more broadly. Still belongs to 5.6b's scope. **ESCALATED (2026-07-17, batch-12 diagnostic)**: re-ran the sweep 2x at `HYDRATION_SWEEP_CONCURRENCY=1` (batch-5.6's gold-standard, single-worker condition) against the same build already used for the earlier 133/131/133 runs, to rule out CPU-contention-from-parallel-workers as the sole cause. Neither run hit 135/135: run 1 was 134/135 (`/login` failed on `net::ERR_NETWORK_IO_SUSPENDED`, a network-stack error unrelated to hydration, not a `HYDRATION_MISMATCH_PATTERNS` match — likely environment noise); run 2 was 134/135 with `/tools/ai` failing on `Minified React error #421`, which *does* match the hydration-mismatch pattern list and is the exact React-error family this whole item tracks. Since concurrency=1 still produced a genuine hydration-race failure (on a different route than either prior run), the race is not just parallel-worker measurement noise — it reproduces even under the single-worker condition, meaning root cause isn't fully understood and it may be intermittent enough to hit real low-end/throttled devices in production too. Batch 5.6b is now the next priority batch after Batch 12 merges.
- **2026-07-16** — **Tips inline-linking** (Batch 12 owner ruling) — `toolGuideContent.js`'s per-tool `tips`/`mistakes` arrays are plain strings with no links to related tools, unlike the reference HTML's Tips section (`docs/reference/tool-page-merge-pdf.html`) which links out to `/tools/rotate-pdf`, `/tools/compress-pdf`, etc. per Blueprint §26's workflow-linking rule. Deferred out of Batch 12 by owner ruling; needs its own batch to add inline `<Link>`s across the existing tips/mistakes content.
- **2026-07-19** — **`client/public/cover-letter-generator.html` is a standalone static page outside the SSG shell** (own `<head>`, doesn't use `index.html` as a template) — found while verifying batch 16's Umami tag reached every `dist/*.html` file; it's the only one of 127 that doesn't. No regression (it never had GA4 either), but it also won't get Umami tracking unless someone wires it in separately. Not fixed here — out of scope.
- **2026-07-16** — **`toolPageContent.js` orphan cleanup (Batch 12C)** — 213KB file, `TOOL_ABOUT` export, 38 keys, but only **5** of the 48 live tool pages still import it (`WordCounter.jsx`, `pdf/PdfEditor.jsx`, `SIPCalculator.jsx`, `BMICalculator.jsx`, `pdf/MergePDF.jsx` — confirmed via grep during Batch 12A, corrects Batch 12's original "only WordCounter.jsx" audit claim). Every other tool has migrated to a local `ABOUT` const. Needs a migration batch: move the 5 remaining tools to local `ABOUT` consts, then delete the file (bundle dead weight).
- **2026-07-16** — **`ai-content-writer` and `resume-builder` don't use `ToolPageShell`** — found during Batch 12A's coverage grep (46/48 tool pages picked up the new updated-date line + author box, these 2 didn't). Both use `ToolLayout` from `components/tool-engine/` instead (Pro-gated AI tools with a different, auth-aware UX pattern). Batch 12B's limitation-line rollout and any future `ToolPageShell`-driven template additions need a separate plan for these 2 — either a parallel addition to `ToolLayout`, or an explicit decision to leave them out.
- **2026-07-17** — **`hydration-sweep.js`'s hydration-mismatch regex misses #421-#425** (found during Batch 5.6b's root-cause investigation) — `HYDRATION_MISMATCH_PATTERNS`'s `/minified react error #41[0-9]/i` only matches error codes 410-419; the adjacent comment claims it also covers #421/#422/#423/#425 (all of which start with "42", not "41"), but the regex never matches them. Harmless to pass/fail correctness (any console error fails a route regardless of pattern match — the list only picks the display label), so no sweep verdict has ever been wrong because of it, but every tool-page hydration failure to date has been mislabeled "console error" instead of "hydration mismatch" in sweep output. One-character-class fix (`#4[12][0-9]` or similar); same regex is duplicated in `hydration-stress.js` (Batch 5.6b). Out of scope for the root-cause investigation itself.
- **2026-07-18** — **Mobile Lighthouse performance remains 83/100** (production, post-Batch-5.6b) — desktop is 100/100/100/100 and mobile's CLS is 0 (confirms 5.6b's hydration fix holds in production), but mobile's non-CLS performance score is capped by route code-splitting / unused JS. Unchanged by 5.6b (that batch fixed hydration correctness, not bundle size); needs its own scoped investigation into which routes ship the most unused JS to mobile and whether further chunk-splitting is viable.
- **2026-07-19** — **City pages differentiation (post-AdSense)**: `docs/reports/city-pages-audit-2026-07.md` found bucket (a) (genuinely differentiated) empty — all 24 city pages noindexed in Batch 15. Revival bar for re-indexing any of them: embed the actual calculator on the page (currently `CityToolPage.jsx` only links out to the generic `/tools/{slug}` page), use city-level (not state-level) verified data with named sources, fix/remove the unsourced Delhi "41.6%" figure, and write substantially unique prose per city — only then selective re-indexing.
- **2026-07-19** — **`cityPages.js` dead `metaTitle`/`metaDescription` fields** — every one of the 24 page objects carries these fields (e.g. `"Free BMI Calculator Ahmedabad 2026 | AWE-OS"`) but `CityToolPage.jsx` never reads them (confirmed: no `<Helmet>` usage in that component at all, found during the city-pages audit). Dead data since the page was built; fold into a future cleanup batch — either wire them into a `<Helmet>` block (if/when the page is re-indexed) or delete them.
- **2026-07-27** — **Raw `bg-blue-600` (not the `--cobalt` token) in 77 files, now visibly inconsistent post-Batch-17** — grepped during Batch 17's screenshot review: `client/src/pages/tools/*`, most `pages/*.jsx`, and all `modules/admin/*` never adopted the design-token system (`bg-cobalt`/tokens.css), confirming and extending Batch 8's earlier finding that only Header/Footer/StatsStrip/homepage sections use it. Harmless while cobalt and the intended action-blue were the same hue; now that Batch 17 repointed `--cobalt` to teal, every one of these 77 files' buttons/links/accents stay old-blue, most visibly the blog post inline tool-CTA (`blogPosts.js`, e.g. the "budget-calculator →" button on `/blog/how-to-create-a-budget-that-works-for-you-in-india`) sitting right below teal-branded nav/hero content. Public-facing tool pages are the priority subset (internal `modules/admin/*` is out of scope per CLAUDE.md's protected zones anyway). Needs its own migration batch — raw-Tailwind-blue -> `bg-cobalt`/`text-cobalt` swap, scoped to public pages first.
- ~~**No sitewide cookie-consent/GDPR banner exists**~~ — **resolved 2026-07-28 (Batch 18)**: `CookieConsentBanner.jsx` + `useCookieConsent.js` added, mounted in `PublicLayout.jsx`. Blocks Umami analytics (`client/index.html`'s unconditional `<script>` removed) until the visitor clicks Accept; Reject persists and never loads it. See `docs/batches/batch-18-cookie-consent-banner-plan.md`.
- **2026-07-28** — **6 pages flagged AI_CONTENT_RISK_HIGH by `audit-awe-os-v2.js`** (`/blog`, `/tools/sip-calculator`, `/tools/currency-converter`, `/blog/merge-pdf-files-for-bank-documents-india`, `/blog/sip-calculator-mutual-fund-returns-india`, `/blog/new-vs-old-tax-regime-fy-2025-26`) — the check (`audit-awe-os-v2.js:213`) is a pure text-statistics heuristic (avg sentence length >18 words AND lexical diversity <0.4), not semantic AI-detection. Owner declined to fix by rewriting prose: doing so would mean generating replacement tool-page/blog copy specifically to satisfy a validator, which CLAUDE.md §7 explicitly forbids ("never generate filler prose to make validators pass" — these pages are exactly the human-written-content category that rule protects). Not blocking: AdSense checklist already passes at 95% (20/21) without this fixed. If ever revisited, needs owner-supplied or owner-approved rewritten prose, not model-generated content.
- **2026-07-28** — **`toolCatalogue.js`'s `calculators.count: '6+'` is stale** — found while adding HRA/NPS/Capital Gains (Batch 19). The Calculators mega-menu category card has shown a hardcoded `'6+'` count since at least Batch 4/5, but `TOOL_REGISTRY` already had 13 live calculators before this batch and now has 16. Same category of drift as the other hardcoded catalogue metadata already flagged elsewhere in this file — needs either a manual bump or (better) deriving `count` from `getToolsByCategory('calculators').length` in `toolRegistry.js` at render time instead of a static string. Not fixed here — out of Batch 19's scope.
- **2026-07-28** — **Tool-page layer never migrated to design tokens (Batch 20)** — `ToolPageShell.jsx` and all 49 tool-page components (`MergePDF.jsx`, `PdfEditor.jsx`, etc.) use raw Tailwind (`bg-blue-600`, `text-gray-900`, ...) instead of `design-system/tokens.css`, unlike `Header.jsx`/nav which are token-based. `architecture.md` §14 only flagged this debt for `ToolsPage`/`CategoryPage`; it actually extends to the entire tool-page template layer. Batch 20's 2 new tools intentionally match the existing raw-Tailwind convention (per CLAUDE.md §5, "match existing patterns") rather than introducing a third styling approach — needs its own migration batch eventually, same family as the item 42 above.
- **2026-07-28** — **`toolCatalogue.js` nav data has drifted from `toolRegistry.js` beyond calculators (Batch 20)** — `pdf-editor` (added to the registry pre-Batch-20) was entirely missing from the PDF mega-menu until this batch added it; several other registry tools (e.g. `gst-calculator`, `tip-calculator`, `currency-converter`, `base-converter`, `json-formatter`, `invoice-generator`) are still missing from their category's mega-menu section. Needs a dedicated sync pass, ideally deriving the catalogue from `toolRegistry.js`'s `getCatalogueSections()` instead of hand-maintaining a parallel file.
- **2026-07-28** — **`pdf-editor`'s `TOOL_ABOUT` content claims "Real-time collaboration"** (found while adding the Extract Text feature in Batch 20) — this is factually false: `PdfEditor.jsx` is a client-side-only, single-user tool with no server, no sync, and no multi-user session of any kind. Needs a content correction (not fixed here — CLAUDE.md §7 treats existing tool-page prose as human-supplied content, not something to silently rewrite mid-batch).
- **2026-07-28** — **`ssg-build.js` occasionally injects 1-2 literal null bytes (`\x00`) into rendered HTML output** — found while verifying Batch 20's `text-editor` page byte-for-byte (2 null bytes right before an em dash in the limitation text, confirmed absent from the JSX source via a byte-level Node scan). Confirmed pre-existing and NOT specific to Batch 20's new content: a full scan of all 130 built `dist/**/index.html` files found the same defect in 3 other pages this batch never touched — `tools/gst-calculator` (1), `tools/pdf` category page (2), `tools/ppf-calculator` (1). Low real-world impact (browsers render stray NULs as U+FFFD per the HTML spec, not a crash), but it's a real serialization bug in the SSG pipeline, not a content issue — needs its own investigation into `ssg-build.js`'s HTML string handling (possibly around specific Unicode sequences like em dashes) rather than a per-page fix.
- **2026-07-29** — **`PdfEditor.jsx`'s text/note/callout annotations push one undo-history entry per keystroke** (found during Batch 23's Phase 0 audit) — the `onChange` wired into `AnnotationEl` (`PdfEditor.jsx`, "onChange={upd=>{pushHistory();updateAnn(...)}}") calls `pushHistory()` on every character typed, so typing a 20-character sentence creates 20 separate undo steps for one logical edit. Pre-existing, not one of Batch 23's 6 approved fixes — needs its own scoped fix (e.g. debounce/commit-on-blur history push) rather than a mid-batch change to shared history-call sites Batch 23 already touched.
- **2026-07-29** — **`PdfEditor.jsx` line/arrow/dashed/measure and polyline annotations have oversized/thin click hit-areas** (found during Batch 23's Phase 0 audit) — line-type annotations' wrapper `div` sets `pointerEvents:'none'` with only the thin SVG stroke itself clickable (`AnnotationEl`, ~line 589 area), and polyline's wrapper SVG spans the *entire page* rather than its own bounding box (~line 609 area), both pre-existing and outside Batch 23's approved scope (the 3 reported bugs + B1/B3/B8/B10). Needs its own pass over annotation hit-area sizing, ideally alongside the Phase-1/2 structural refactor already discussed for this file.
- **2026-07-29** — **PDF Editor Phase 5b deferred** (batch-28 planning) — virtualized page rendering, Web Worker pdf-lib export, touch/pinch-zoom on the main canvas, and a real dark theme (extending beyond the current canvas-backdrop-only `darkCanvas` toggle to the ribbon and right properties panel) all need real browser/device verification before merging — none of them can be safely implemented blind given the assistant has no browser-automation tool in this environment, and each carries real regression risk (broken export, illegible dark-mode text, broken virtualization/scroll). Needs its own batch with a human doing the verification pass, not folded into batch-28's safe/additive items.
- **2026-07-29** — **PDF Editor Phase 5c (accessibility pass) deferred** (batch-28 planning) — `PdfEditor.jsx` has zero `aria-label`s on its dozens of icon-only buttons (ribbon tools, delete "✕" buttons, modal close buttons), no keyboard path to select/move/resize/delete an annotation (100% mouse-only), and the canvas has no accessible fallback text. Flagged in the original batch-23 audit as likely failing this repo's CI-enforced a11y gates (CLAUDE.md §5). Broad enough to deserve its own focused batch rather than being folded into Phase 5's tail.
